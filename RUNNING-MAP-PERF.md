# Running 轨迹地图加载性能诊断与优化方案

> 状态：待审阅（节 1 诊断 + 节 2 方案对比，确认方向后再实施）
> 日期：2026-08-24

---

## 节 1 · 现状诊断（数据实测 + 代码定位）

### 1.1 数据规模（实测 activities.json）

| 指标 | 数值 |
|---|---|
| 总记录数 | 161（全部 Ride 骑行） |
| 带 summary_polyline | 161 / 161 |
| 解码后总点数 | **≈ 36.2 万** |
| 平均点数 / 条 | 2,248 |
| 单条最多点数 | 14,520 |
| 文件体积 | 829 KB（精简字段仅能减 ~10%，大头是轨迹数据本身） |

### 1.2 瓶颈定位（按成本排序）

| # | 瓶颈 | 代码位置 | 说明 |
|---|------|----------|------|
| 1 | **fit() 缩放循环全量重投影** | `rkMapInit` L2834-2846 → `setZoom` L2755-2763 | 初始渲染从 z14 开始逐档缩放，**每降一档就对全部 36 万点重新做墨卡托投影 + 抽稀 + bbox**，是首屏卡顿的主因 |
| 2 | **每次渲染全量重解码 polyline** | `rkShowMap` L2643 → `rkMapTracks` L2620-2629 | 进页面、点列表、切样式、明暗切换都会重新解码 36 万点 |
| 3 | **setZoom 先投影后抽稀** | L2760-2761 | 36 万点全部投影后才抽稀；抽稀是等步长采样、与投影无关，**应先抽稀再投影**，投影量直接降 ~11 倍 |
| 4 | **SVG 字符串 + innerHTML 重建** | `render` L2794-2820 | 全局点预算 9 万，拼 ~180 万字符的 points 字符串再 innerHTML 解析，主线程阻塞 |
| 5 | **数据源单点依赖** | `RK_URL` L2188 + `rkFetch` L2930-2952 | 直连 raw.githubusercontent.com，国内不稳定；当前缓存策略「有缓存才用、无缓存硬等」 |
| 6 | 热点视角每次重算 | `rkHotSpot` L2682-2736 | 数据不变时结果恒定，可缓存 |

### 1.3 关键优化前提（两个数学事实）

1. **等步长抽稀与投影无关**：`rkThin` 按索引均匀采样，在经纬度上先抽稀、再投影，效果等同，计算量降一个量级。
2. **墨卡托坐标随 zoom 线性缩放**：zoom z 下的世界像素坐标 = 基准 zoom z₀ 坐标 × 2^(z−z₀)（x、y 均线性）。因此**只需在基准 zoom 投影一次，fit/缩放/bbox 全部变成 O(1) 乘法换算**，不再需要任何三角函数。

---

## 节 2 · 优化方案对比

| 方案 | 内容 | 改动量 | 预期收益 | 风险 |
|------|------|--------|----------|------|
| **A · 快速补丁** | 仅修数据通道：多源探测 + 缓存先行渲染 | ~20 行 | 首屏网络等待显著下降；渲染仍卡 | 无 |
| **B · 渲染管线重构（推荐）** | A + 先抽稀后投影 + 基准投影缓存 + bbox 缓存 + 解码缓存 | ~70 行 | **首帧渲染从秒级 → 几十 ms**；缩放/平移交互顺滑 | 低（逻辑纯函数化，verify 可测） |
| **C · 全量升级** | B + SVG → Canvas 2D 重写 + LOD 分层 | ~250 行 | 体验上限最高，万点级实时交互 | 中（交互重写，回归面大） |

### 推荐：方案 B（+ A 的内容）

- **A1 多源自动探测**：`RK_URL` 改为候选数组 `[raw.githubusercontent, cdn.jsdelivr, fastly.jsdelivr, gcore.jsdelivr]`，取最先成功者（复用 skills 页 probes 模式 L1590）。*注意：本机实测 raw 反而最快，不能简单替换为单一 CDN，必须自动探测。*
- **A2 缓存先行（stale-while-revalidate）**：Cache API 有缓存 → 立即用旧数据渲染（秒开），后台拉新成功后更新缓存并重渲染。
- **B1 解码缓存**：`rkMapTracks` 将 `coords` 挂回活动对象（`a._coords ??= rkDecodePolyline(a.poly)`），点击列表/切样式/切页面不再重复解码 36 万点。
- **B2 先抽稀后投影**：经纬度坐标先 `rkThin(200点/条)`，再投影 → 投影量 36 万 → ~3.2 万。
- **B3 基准投影 + 线性换算**：数据就绪时在基准 z₀=14 投影一次，`setZoom`/`bbox`/`fit` 全部改为 ×2^(Δz) 换算，`fit()` 的 while 循环不再有任何重投影。
- **B4 热点视角缓存**：`rkHotSpot` 结果挂全局，数据不变不再重算。

### 预计效果

| 指标 | 现状 | 方案 B 后 |
|------|------|-----------|
| 首帧地图渲染（CPU） | 数秒（多轮 36 万点全量投影） | ~30-80 ms |
| 点击轨迹 / 切换样式 | 每次重新解码 + 渲染，卡顿 | 毫秒级增量渲染 |
| 首次数据加载（网络） | 依赖 raw 单点，可能超时 | 多源探测，秒级 |
| 二次访问（缓存） | 仍需等待检查 | 缓存先行，秒开 |

---

## 节 3 · 实施计划（确认后执行）

1. 改 `RK_URL` → `RK_SOURCES[]` 多源探测 + `rkFetch` 缓存先行（L2188, L2930-2952）
2. 改 `rkMapTracks` 解码缓存（L2620-2629）
3. 改 `rkMapInit`：基准投影缓存 + 线性换算 + 先抽稀后投影（L2737-2927 内 setZoom/bbox/fit/render）
4. `rkHotSpot` 结果缓存（L2682）
5. 跑 `verify.js` 213 条回归 + 手工实测 /running 页
6. 同步 `404.html`（pre-commit 钩子自动处理）

> 可选：方案 C（Canvas 重写）留作后续，若方案 B 后仍嫌交互不够流畅再评估。
