# macOS
.DS_Store

# JetBrains IDE
.idea/

# 临时验证脚本
.tmp-*.js

# 依赖（若本地引入）
node_modules/

# 环境变量（勿提交任何密钥）
.env
.env.local

# 生成物（test-worker.mjs 运行时自动 cp worker.js 生成；生成脚本已删，条目保留防误提交）
worker.test.mjs

# Playwright 本地运行产物（e2e 报告 / 失败截图与 trace）
playwright-report/
test-results/

# 本地工具数据（WorkBuddy 工作日志/产物，不入库）
.workbuddy/

# running 数据仓库（本地开发用普通 clone 管理，不再作为 submodule 发布到 Pages）
/running/

# running-private 私有仓库（本地开发 clone，独立仓库）
/running-private/

# Cloudflare wrangler 本地缓存（部署产物，勿提交）
.wrangler/

# Qwik 构建产物
dist/
/lib/
/server/
app/dist/
app/server/
app/lib/
.qwik/
tsconfig.tsbuildinfo

# 像素素材母版与临时产物（脚本已归档到 tools/pixel-art/）
.tmp-art/
