# 06 · 集成 / 生产验证

## 前置（Turn B 完成后）
用户需先在 Dashboard 加自定义域 `api.guoxin.space` + 改 OAuth callback。

## Worker 层（curl / Playwright 直连）
```bash
# 沙箱 curl 不通 workers.dev / 自定义域时改用 Playwright chromium --no-proxy-server
curl -s https://api.guoxin.space/api/health                       # 期望 200 {ok:true,...}
curl -s https://api.guoxin.space/api/tracks/raw?f=preview.json    # 期望 200（TRACKS_REPO 变量在）
```

## 站点层（Playwright 生产自测）
- 访问 `https://guoxin.space/todo`：登录门禁正常、登录后 TODO 读写经 `api.guoxin.space`。
- 访问 `https://guoxin.space/skills`：列表加载 + 「测试连接」绿勾。
- 访问 `https://guoxin.space/running`：轨迹 preview 加载正常。

## 已完成的前置验证（Turn A 阶段）
- `guoxin-space.lgx31.workers.dev/api/health` → **200**（`admin:true`，变量在）。
- `guoxin-space.lgx31.workers.dev/api/tracks/raw?f=preview.json` → **200**（真实轨迹）。
- `skillboard-collect.lgx31.workers.dev/api/health` → **404**（旧域名确认失效）。

## 状态
- [ ] Turn B 用户操作完成
- [ ] 生产 Playwright 自测全绿（待补）
