# 06 · 集成 / 生产验证

## Worker 层（已完成的实测，Playwright chromium `--no-proxy-server` 直连）
| URL | 结果 |
|---|---|
| `https://guoxin-space.lgx31.workers.dev/api/health` | **200** `{ok:true, repo:"guoxinl/skill-collection", admin:true}` → 变量保留 |
| `https://guoxin-space.lgx31.workers.dev/api/tracks/raw?f=preview.json` | **200** 真实轨迹数据 → `TRACKS_REPO` 变量在 |
| `https://skillboard-collect.lgx31.workers.dev/api/health` | **404** → 旧域确认失效 |

> 沙箱 `curl` 直连 workers.dev 被拦截（返回 000），改用 Playwright 浏览器 `--no-proxy-server` 直连验证。

## 站点层（待补）
- 访问 `https://guoxin.space/todo`：登录门禁正常，登录后 TODO 读写经 `guoxin-space.lgx31.workers.dev`。
- 访问 `https://guoxin.space/skills`：列表加载 + 「测试连接」绿勾。
- 访问 `https://guoxin.space/running`：轨迹 preview 加载正常（CI e2e 亦覆盖此页）。

## 状态
- [x] Worker 默认域可达、变量齐全
- [x] 站点部署 e2e 转绿：run **35225973512 success**
- [x] 生产 Playwright 自测（2026-09-17 21:18）：`/running` 数据经新默认域加载、`/todo` 门禁正常、`/skills` 可加载
- [ ] OAuth callback 更新（用户；改后 /todo 登录可通）
