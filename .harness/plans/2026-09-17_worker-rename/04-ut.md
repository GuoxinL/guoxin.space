# 04 · 单元测试（门禁）

触发原因：改动 `app/src/lib/skills.ts`（`SK_DFLT_WORKER` 常量）→ 必跑 `pnpm test`。

## 执行
```bash
cd /Users/guoxin/code/github/guoxin.space
CI=true pnpm test
```

## 结果
- 首跑：**1 失败** — `src/lib/auth.test.ts:89` 断言默认 Worker 含 `workers.dev`，与新的 `api.guoxin.space` 不符。
- 修复：`auth.test.ts:89` 断言改为 `toContain('api.guoxin.space')`。
- 复跑：**Test Files 21 passed · Tests 310 passed** ✅

## 说明
- `running.test.ts:284` 断言同步为新默认域名，通过。
- `worker.test.mjs`（根目录 Worker 脚本级）不在 vitest 门禁内；Worker 行为以线上 curl 验证（见 06-it）。
