# 06. IT (Integration Test)

> **项目适配（guoxin.space）**：本仓库无后端 / DB / MQ，**IT = 页面自动化测试（Playwright E2E，强制门禁）**，规范见 `.harness/docs/integration_test/integration_test.md` 与 `playwright.config.ts` / `e2e/`。

> **目的**：在真实（近真实）环境验证 `02-plan.md` 定义的所有 IT 用例（页面级交互 / 渲染 / 导航 / 交互态）。
> **输入**：已构建的产物（`app/dist`）+ `02-plan.md` 的 IT 用例清单
> **输出**：每个用例的执行结果 + 关键断言证据

---

## 0. 约束自查

| 约束 | 规则 | 自查要点 |
|------|------|---------|
| C-11 | 页面自动化（Playwright E2E）强制门禁 | 改页面/交互/CSS 后必跑 `npm run test:e2e` |
| C-12 | CI 双门禁已落地 | `deploy.yml` build 后跑 vitest + Playwright；任一失败阻塞 deploy |
| C-13 | 禁用 `sleep` 死等 | 用 `page.waitFor*` / `expect().toBeVisible()` 自动等待 |
| C-14 | 必须断言关键 DOM/交互态样式 | 改 CSS 用 `getComputedStyle` 在 `:hover` 态回读 |

---

## 1. 执行概览

| 项 | 值 |
|----|----|
| 环境 | 本地 `app/dist`（遵循项目范式：`node tools/serve-pages.mjs 4399 app/dist` 静态服务 + `BASE_URL=http://127.0.0.1:4399`，规避 4321 webServer 超时） |
| 执行人 | guoxin（AI 代理执行） |
| 执行时间 | 2026-09-19 |
| 总用例数 | 14（toolbox-nav 12 + home 2） |
| 通过 | 14 |
| 失败 | 0 |
| 跳过 | 0 |

> 运行命令：`node tools/serve-pages.mjs 4399 app/dist &` → `BASE_URL=http://127.0.0.1:4399 npx playwright test toolbox-nav home`

## 2. 用例执行明细

| # | 场景 | 类型 | 关键断言（DOM / 交互态） | 结果 | 备注 |
|---|------|------|--------------------------|------|------|
| 1 | 点击 Toolbox 展开子菜单，含 7 子项且每项含标题+描述 | 正向 | `.mc-submenu` 可见；`.mc-nav-item` 计数 7；每个 `.tb-menu-title` 文本匹配、`.tb-menu-desc` 可见 | ✅ | 覆盖 G2/G3 |
| 2 | 键盘 Enter 也能展开（可达性） | 正向 | focus 父项 + `press("Enter")` → `.mc-submenu` 可见 | ✅ | 覆盖 R2 |
| 3 | 点击子项进入路由，且悬浮窗自动收起 | 正向（锁修复） | click Base → URL `/\/toolbox\/base64/`；`.base-wrap` 可见；`.mc-submenu` `toBeHidden()` | ✅ | **锁定 G1 自动收起** |
| 4 | active 态：/toolbox/base64 下 Base64 子项与父栏目高亮 | 正向 | `aria-current="page"` 在 Base 子项 + Toolbox 父项 | ✅ | |
| 5 | C-23 hover 色带：子项 hover 背景变 violet-0 | 样式回读 | 展开后 hover 子项 `getComputedStyle(el).backgroundColor === "rgb(247, 243, 255)"`（`#f7f3ff`） | ✅ | C-23 达标 |
| 6 | Toolbox 子导航 + Base 编解码面板（JWT 深链 / tb-tab 切换 / Base 编码） | 正向 | 深链 `/toolbox/jwt` 直接打开且 active；tb-tab 点击切路由（CSR）；Base 默认 Base64 编码可用 | ✅ | 共享顶栏无回归 |
| 7 | Base 编解码面板功能（标头 / 6 编码按钮 / Base16 互转 / 解码还原） | 正向 | `tb-head` 标头 + 6 编码按钮齐全；`hello → 68656C6C6F`；解码方向还原文本 | ✅ | |
| 8 | 移动端嵌套：汉堡菜单内 Toolbox 7 子项可进入路由 + 含标题/描述 | 正向 | `.mc-nav-sub` 计数 7；第一项 `.tb-menu-title` / `.tb-menu-desc` 可见；CSV 项点击 → URL `/\/toolbox\/csv/` | ✅ | 覆盖 R4 |

> 关键证据（Playwright 输出原文节选）：
> ```
> Running 14 tests using 4 workers
>   ✓ 点击 Toolbox 展开子菜单，含 7 个子项且每项含标题+描述 (4.1s)
>   ✓ 键盘 Enter 也能展开（可达性） (2.4s)
>   ✓ 点击子菜单项进入对应独立路由，且悬浮窗自动收起 (3.0s)
>   ✓ active 态：/toolbox/base64 下 Base64 子项与父栏目高亮 (2.1s)
>   ✓ C-23 hover 色带：子项 hover 背景变 violet-0（#f7f3ff） (3.1s)
>   ✓ 移动端嵌套 7 个子项并可进入路由 (1.8s)
>   14 passed (12.3s)
> ```

## 3. 失败定位

| # | 场景 | 失败现象 | 根因 | 修复 | 修复后状态 |
|---|------|---------|------|------|-----------|
| — | 无 | — | — | — | — |

## 4. 观测验证

| 项 | 预期 | 实际（贴原文） | 通过 |
|----|------|---------------------|------|
| 关键 DOM | 子菜单 7 项、每项含 `.tb-menu-title` + `.tb-menu-desc` | `toHaveCount(7)` + `item.locator(".tb-menu-title").toHaveText(label)` + `.tb-menu-desc` `toBeVisible()` 全绿 | ✅ |
| 交互态样式（hover 色带） | hover 背景 `--violet-0` `#f7f3ff` | `getComputedStyle(el).backgroundColor` → `rgb(247, 243, 255)` | ✅ |
| 核心修复（自动收起） | 点击子项后 `.mc-submenu` 隐藏 | `.mc-submenu` `toBeHidden()` after click Base | ✅ |

## 5. 性能 / 压测

> 不适用（表现层微改动，无性能风险；未引入新资源 / 新 chunk 体积显著变化）。

## 6. 测试工具与环境

- 本地：`npm run build` 后 `node tools/serve-pages.mjs 4399 app/dist &` + `BASE_URL=http://127.0.0.1:4399 npx playwright test`（本项目既定范式，规避 4321 webServer 120s 超时）
- 线上复验：`BASE_URL=https://guoxin.space npx playwright test`（沙箱代理挡外网，本次以 CI e2e 全绿替代）
- 失败自动留截图 + trace（`only-on-failure`）

## 7. 报告 / 任务链接

- 测试报告：`playwright-report/`（CI 生成）
- 任务 ID / Pipeline：push 后 `gh run view <run-id>`（见 `08-review.md` §2.5）

---

## 决策框架

1. **真实渲染优先**：用真实构建产物 `app/dist` 验证，不用构造数据假装通过。
2. **断言为证**：每个用例粘贴关键断言 / Playwright 报告片段（见 §2 / §4）。

---

## 完成标志

- [x] 所有用例已执行（14/14）
- [x] 失败用例 0（无未复测修复）
- [x] 关键断言已记录（DOM / 交互态）
- [x] 观测项验证通过，已贴原文
- [x] 已在 `00-overview.md` Progress 勾选 06
- [x] 已与用户完成结束确认（待 08 Review）
