# 04. UT

> **项目适配（guoxin.space）**：本仓库 UT 用 **Vitest**（非 Go `go test`）；规范与环境见 `.harness/docs/unittest/unittest.md`。下方 `testcontainers` / `go test` 等示例为通用模板残留，**本项目不适用**（静态站无后端 / DB）。

> **目的**：以单元测试证明行为正确、回归可防。
> **输入**：`02-plan.md §6` UT 用例设计 + `03-implement.md` 完成的代码
> **输出**：测试文件 + 覆盖率报告 + 本文件

## 0. 约束自查（强制，详见 `.harness/docs/CONSTRAINTS.md`）

- 测试门禁：C-10（UT 强制，改 lib 必跑）、C-15（全 Mock / 用例独立）
- 编码：C-30（类型严格，禁 any）、C-31（Qwik 原语）

---

## 0.1 前置阅读（强制）

1. **`02-plan.md` §6 UT 用例设计** — 本任务 §6 明确"无新增单测：改动为表现层（Header 组件 + global.css + e2e 契约），无 `app/src/lib` 纯逻辑变更，UT 不适用；交互/渲染正确性由 `06-it.md` 页面自动化覆盖"。
2. **`.harness/docs/unittest/unittest.md`** — Vitest 环境 / 生成规范 / 运行命令。
3. **Vitest 运行环境** — Node ≥24（`npm run test`）；单测目录 `app/src/{lib,components}/**`；无 DB，依赖一律 mock。

---

## 0.5 TDD 工作流自检

| 阶段 | 期望状态 | 当前情况 |
|------|---------|---------|
| 🔴 Red | Plan §6 列出的 UT 已先写、首次跑全部失败 | ⏭ 跳过（本任务无 lib 逻辑变更，Plan §6 已判定 UT 不适用） |
| 🟢 Green | 实现使所有 UT 转绿 | ✅ 既有 Vitest suite 不受影响，全绿 |
| 🔵 Refactor | 在 UT 保持绿的前提下重构代码 | ⏭ 无需重构 |

> 跳过 Red 原因：本任务改动完全落在 Qwik 组件渲染 + CSS + e2e 契约，属表现层；`Header.tsx` 的 `tbOpen` 状态逻辑与 DOM 事件紧耦合，单元测试价值低且项目既有 e2e 已锁定关键行为（自动收起、标题/描述、hover 色带）。正确性由 `06-it.md` E2E 兜底，符合项目"改页面/交互/CSS 必跑 e2e"的硬门禁（C-11）。

---

## 1. 用例清单

> 本任务**无新增 Vitest 用例**（Plan §6 已判定 UT 不适用）。

| # | Plan §6 编号 | 用例名 | 测试文件 | 类型 | 状态 |
|---|-------------|--------|---------|------|------|
| — | §6-1 | 无（表现层改动，UT 不适用） | — | — | ⏭ 跳过 |

> 既有 Vitest suite（`npm run test`）在改动后保持全绿，未因 `Header.tsx` / `global.css` 改动引入失败。

## 2. 执行命令

```bash
npm run test        # 全量单测（CI 同款）；本次无新增用例，既有 suite 全绿
```

## 3. 结果

| 项 | 值 |
|----|----|
| 通过 / 总数 | 既有 suite 全绿（无新增用例） |
| 覆盖率 | 本次无新增被测逻辑，覆盖率无变化 |
| 门槛 | 当前**无强制覆盖率阈值**（见 `unittest.md` §5） |

**未覆盖行**：
```
无新增逻辑，故无未覆盖行。tbOpen 状态机与 DOM 事件由 e2e 覆盖（06-it.md）。
```

## 4. 失败定位

| 用例 | 失败原因 | 修复动作 | 修复后状态 |
|------|---------|---------|-----------|
| — | 无失败 | — | — |

---

## 用例设计清单（Checklist）

> 本任务无 lib 纯函数改动，下表按"表现层"重释：

### 输入维度（e2e 已覆盖）
- [x] 点击 Toolbox 触发器展开 / 再次点击收起
- [x] 键盘 Enter 展开（可达性）
- [x] 点击子项进入路由 + 悬浮窗自动收起（核心修复锁）
- [x] 路由变更自动收起 / 外部点击收起 / Escape 收起

### 状态维度
- [x] 默认态（无 `.is-open`）子菜单隐藏
- [x] hover 态 `.mc-nav-item` 背景 `--violet-0`（C-23 回读）

### 依赖维度
- [x] 无外部依赖（纯前端组件）

---

## 禁止事项（红线）

- ❌ 改业务代码让测试过
- ❌ 调用真实外部服务 / Worker（e2e 亦为本地静态服务 `app/dist`，无外网依赖）
- ❌ 用例间依赖执行顺序

---

## 完成标志

- [x] 无新增用例（Plan §6 已判定不适用，并记录原因）
- [x] 既有 Vitest suite 全绿，未引入失败
- [x] 已在 `00-overview.md` Progress 勾选 04.
- [x] 已与用户完成结束确认（待 08 Review）
