# 04 UT — 单元测试结果

执行：`npm run test`（Node 24 + `CODEBUDDY_SAFE_DELETE_ENABLED=0`）

## 结果：✅ 265 passed / 0 failed（16 files）

## 新增/扩展单测
- `lib/json/schema.test.ts`（7）— inferSchema 各类型 / 对象属性 / 数组合并 / 混合退化
- `lib/json/smalltools.test.ts`（10）— Base64 中文 roundtrip / 非法报错 / URL / 时间戳口径 / JWT / CSV 引号与 roundtrip
- `lib/json/structdiff.test.ts`（5）— 新增/删除/变更 / 嵌套 / 数组下标 / 类型不同
- `lib/calendar/calendar.test.ts`（+5）— `nextHoliday` 天数差 / 当天=0 / 年后 null / 年前定位 / 未维护年份诚实返回 null
- 历史会话已覆盖：`ops.test.ts`（repair lang）、`jsonpath.test.ts`（词边界）、`share` encode/decode

## 断言修正说明
- 混合类型数组 `inferSchema([1,'x'])` 实现选择「省略 items」（无约束数组），单测断言由 `{items:{}}` 改为 `items` 为 undefined —— 反映真实设计，非实现缺陷。
- `diffStructure` 同键两侧均存在时 `removed=0`，单测原期望 0 误写为 1，已修正为正确语义。
