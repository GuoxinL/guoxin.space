# 07 Docs — 文档更新

本次改动属功能增强，无对外 API / 架构变更，文档更新集中在 SOP 步骤文件（本目录 02–08）与既有约定保持一致：

- 设计系统：新增 CSS 类均复用既有 token（`--violet-0` / `--violet-65` / `--slate-*` / `--success-*`），未改动 `.btn` 基类与 `--container-w` 单点开关（符合 DESIGN.md / AGENTS.md 铁律）。
- 文案口径：日历 intro 延续「农历 · 法定节假日与调休 · 节气」；「距下一假期」提示条沿用诚实口径（无数据不显示，不臆造 2027）。
- 无新增第三方依赖（C-08 隔离环境风险规避）。

> 若后续要写用户可见的「小工具使用说明」，建议放在 `/toolbox/json` 页内 hint（已含功能列表），不另开文档。
