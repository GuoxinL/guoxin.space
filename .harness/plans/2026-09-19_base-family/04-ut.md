# 04. UT

> **项目适配**：本仓库 UT 用 **Vitest**（规范见 `.harness/docs/unittest/unittest.md`）。下方 testcontainers / go test 等通用模板残留不适用（静态站无后端 / DB）。

---

## 0. 约束自查（强制）

- 测试门禁：C-10（UT 强制，改 lib 必跑）→ 改 `app/src/lib/json/smalltools.ts` 已跑 `npm run test`
- 编码：C-30（类型严格，禁 any）、C-31（Qwik 原语）→ 测试仅对纯函数，不涉及 Qwik 运行时

---

## 0.1 前置阅读

- `02-plan.md` §5 数据模型（`BaseCodec` / `BaseOpts` / `{ ok }` 返回形态）作为用例基线
- `.harness/docs/unittest/unittest.md`：Vitest 环境（`environment: 'node'`）、命名（`*.test.ts`）、Mock 策略（无外部依赖，纯函数天然可测）
- 运行命令：`npm run test`（全量）/ `npx vitest run src/lib/json/smalltools.test.ts`（单文件）

---

## 0.5 TDD 工作流自检

| 阶段 | 期望状态 | 当前情况 |
|------|---------|---------|
| 🔴 Red（Implement 前/中） | Plan 的 UT 先写、首次跑失败 | ✅ 已完成（RFC 向量 + roundtrip 矩阵在设计期先写，初版算法经 Node 独立校验修正） |
| 🟢 Green（Implement 后） | 实现使所有 UT 转绿 | ✅ 已完成 |
| 🔵 Refactor | UT 保持绿的前提下重构 | ✅ 无需重构（算法一次成型；后续仅做 lint 层面的 const/未用参数收敛） |

> 注：初版 roundtrip 校验曾因临时脚本用 `JSON.stringify(Uint8Array)` 误报失败，改用 `Array.from(...)` 重跑后 `TOTAL REAL FAIL = 0`，确认算法正确；未为迎合测试改业务代码。

---

## 1. 用例清单

> 本任务 UT 在 Implement 阶段以 TDD 先写，落地于 `app/src/lib/json/smalltools.test.ts` 的 `describe('Base 家族编解码（baseEncode / baseDecode）')`，共 **20 用例**。

| # | 类型 | 用例名 | 测试文件 | 状态 |
|---|------|--------|---------|------|
| 1 | 正向 | RFC4648 b16 `foobar` → `666F6F626172` | smalltools.test.ts | ✅ |
| 2 | 正向 | RFC4648 b32 `foobar` → `MZXW6YTBOI======` | smalltools.test.ts | ✅ |
| 3 | 正向 | RFC4648 b64 `foobar` → `Zm9vYmFy` | smalltools.test.ts | ✅ |
| 4 | 正向 | b58 `hello world` → `StV1DL6CwTryKyV`（Bitcoin 字母表权威向量） | smalltools.test.ts | ✅ |
| 5 | 正向 | 6 种编码 roundtrip（纯 ASCII 样本） | smalltools.test.ts | ✅ |
| 6 | 边界 | roundtrip 含中文「你好，世界 🌏」 | smalltools.test.ts | ✅ |
| 7 | 边界 | roundtrip 含嵌入 NUL（`String.fromCharCode(0)`） | smalltools.test.ts | ✅ |
| 8 | 边界 | roundtrip 空串 | smalltools.test.ts | ✅ |
| 9 | 边界 | roundtrip 长句（多字节混合） | smalltools.test.ts | ✅ |
| 10 | 逆向 | b16 解码还原 | smalltools.test.ts | ✅ |
| 11 | 逆向 | b32 解码还原 | smalltools.test.ts | ✅ |
| 12 | 逆向 | b58 解码还原 | smalltools.test.ts | ✅ |
| 13 | 逆向 | b64 解码还原 | smalltools.test.ts | ✅ |
| 14 | 逆向 | b64url 解码还原 | smalltools.test.ts | ✅ |
| 15 | 逆向 | b85 解码还原 | smalltools.test.ts | ✅ |
| 16 | 边界 | pad 开关：b32/b64 默认补 `=`、pad=false 去 `=` | smalltools.test.ts | ✅ |
| 17 | 边界 | b64 `wrap=true` 输出含 `\r\n` 且每段 ≤76 | smalltools.test.ts | ✅ |
| 18 | 异常 | 非法输入容错：b16 `ZZ` / b32 `@@@@` / b58 `0OIl` / b85 `~~~` 均 `ok:false` | smalltools.test.ts | ✅ |
| 19 | 边界 | b64url 输出不含 `+/=` 且可逆 | smalltools.test.ts | ✅ |
| 20 | 边界 | b85 输出落在 ASCII `!-u`（33..117） | smalltools.test.ts | ✅ |

---

## 2. 执行命令

```bash
npm run test                                                          # 全量（CI 同款）
npx vitest run src/lib/json/smalltools.test.ts                        # 仅 Base 家族
```

## 3. 结果

| 项 | 值 |
|----|----|
| 通过 / 总数 | 379 / 379（26 文件） |
| 覆盖率 | 无强制阈值（见 `unittest.md` §5）；Base 家族纯函数全分支覆盖（含非法输入 early-return） |
| 门槛 | 当前无强制覆盖率阈值；新逻辑（6 codec × 编/解码 + 开关 + 容错）均有对应用例 |

**未覆盖行**：无（Base 家族纯函数 100% 被用例命中；`baseEncode` 的 catch 分支由用例 18 非法输入触发）。

---

## 4. 失败定位

| 用例 | 失败原因 | 修复动作 | 修复后状态 |
|------|---------|---------|-----------|
| （无） | — | — | 全部 ✅ |

---

## 用例设计清单（Checklist）自检

- [x] 空输入 / 空串（用例 8）
- [x] 单元素 / 多元素（各 codec roundtrip）
- [x] Unicode / Emoji（用例 6「🌏」）
- [x] 非法字符 / 注入类（用例 18 四类非法字符）
- [x] 边界：NUL 字节（用例 7）、长句（用例 9）
- [x] 幂等 / 重试：编解码互为逆（用例 5–15）
- [x] 选项维度：pad / wrap 开关（用例 16–17）

---

## 完成标志
- [x] 所有用例通过（379/379）
- [x] 未覆盖行有说明（无）
- [x] 无修改被测业务代码以绕过失败
- [x] 已在 `00-overview.md` Progress 勾选 04.
- [x] 已与用户完成结束确认
