# 06. IT (Integration Test)

> **项目适配**：本仓库无后端 / DB / MQ，**IT = 页面自动化测试（Playwright E2E，强制门禁）**，规范见 `.harness/docs/integration_test/integration_test.md` 与 `playwright.config.ts`。

---

## 0. 约束自查

| 约束 | 规则 | 自查要点 | 通过 |
|------|------|---------|------|
| C-11 | 页面自动化（Playwright E2E）强制门禁 | 改任何页面/交互/CSS 后必跑 `npm run test:e2e` | ✅ |
| C-12 | CI 双门禁已落地 | deploy.yml build 后跑 `pnpm test` + `pnpm test:e2e` | ✅ |
| C-13 | 禁用 `sleep` 死等 | 用例用 `expect().toBeVisible()` / `toHaveValue()` 自动等待 | ✅ |
| C-14 | 必须断言关键 DOM/交互态样式 | 改 CSS 用 `getComputedStyle` 在 `:hover` 态回读（C-23 用例） | ✅ |

---

## 1. 执行概览

| 项 | 值 |
|----|----|
| 环境 | 本地 `app/dist`（经 `tools/serve-pages.mjs 4399` 静态服务，Pages 语义） |
| 执行人 | AI（agent） |
| 执行时间 | 2026-09-19 |
| 总用例数 | 33 |
| 通过 | 33 |
| 失败 | 0 |
| 跳过 | 0 |

> 命令：`BASE_URL=http://127.0.0.1:4399 npx playwright test e2e/toolbox-nav.spec.ts e2e/toolbox-json.spec.ts e2e/calendar.spec.ts`（同 shell 内起服 → 跑测 → kill，规避沙箱静态服务随 shell 退出被杀）。

---

## 2. 用例执行明细

| # | 场景 | 类型 | 关键断言（DOM / 交互态） | 结果 |
|---|------|------|--------------------------|------|
| 1 | Toolbox 悬浮子菜单含 7 子项（桌面） | 正向 | `.mc-submenu .mc-nav-item` 计数 7，含 `Base`（标签已统一） | ✅ |
| 2 | 点击子菜单项进入 `/toolbox/base64` | 正向 | 点 `Base` → URL `/toolbox/base64`、`.base-wrap` 可见 | ✅ |
| 3 | active 态：`/toolbox/base64` 下 `Base` 高亮 | 正向 | `.mc-submenu .mc-nav-item`(Base) `aria-current="page"` | ✅ |
| 4 | C-23 hover 色带 | 交互态 | 子项 hover 背景 `rgb(247,243,255)`（violet-0），≠ 静止态白 | ✅ |
| 5 | 深链 `/toolbox/jwt` 直接打开 | 正向 | `.tools-panel` 可见、`.tb-tab[aria-current="page"]`(JWT) 计数 1 | ✅ |
| 6 | 子导航（tb-tab）点击切换路由 | 正向 | 点 `URL` → URL `/toolbox/url`、`.tb-tab`(URL) active 计数 1 | ✅ |
| 7 | Base 编码默认 Base64 可用 | 正向 | `.btn.codec.active`(Base64) 计数 1；输入 `hello` → 输出 `aGVsbG8=` | ✅ |
| 8 | tb-head 标头 + 6 codec 按钮 | 正向 | `.tb-head h2`=Toolbox、`.tb-head p`=Small tools…；`.base-toolbar .btn.codec` 计数 6；`Base16 (Hex)` 存在 | ✅ |
| 9 | 切换 Base16 编码 `hello→68656C6C6F` | 正向 | `.btn.codec.active`(Base16 (Hex)) 计数 1；输入 `hello` → 输出 `68656C6C6F` | ✅ |
| 10 | 切换解码方向还原 `aGVsbG8=→hello` | 逆向 | 点 `← 解码`；输入 `aGVsbG8=` → 输出 `hello` | ✅ |
| 11 | 小工具已迁为独立路由可独立打开 | 正向 | `/toolbox/base64` 直接打开并编码 | ✅ |
| 12 | 移动端汉堡菜单内 Toolbox 嵌套 7 子项 | 正向 | `.mc-nav-sub .mc-nav-item` 计数 7，可进入 `/toolbox/csv` | ✅ |
| 13–33 | JSON / 日历既有用例（10 + 11） | 回归 | 全部 ✅，未受 Base/页头重排影响 | ✅ |

---

## 3. 失败定位

| # | 场景 | 失败现象 | 根因 | 修复 | 修复后状态 |
|---|------|---------|------|------|-----------|
| — | — | — | — | — | 全部 ✅ |

> 初版 e2e 曾有 2 例失败（`toolbox-nav` 的悬浮子菜单/active 断言仍引用旧标签 `Base64`），定位为**测试选择器与实现标签不一致**（实现已统一为 `Base`，但 e2e 旧 `Base64` 残留）——已修正 `SUBMENU_LABELS` 与 active 断言为 `Base`，复测 33 全绿。属测试同步问题，非业务回退。

---

## 4. 观测验证

| 项 | 预期 | 实际（贴原文） | 通过 |
|----|------|---------------------|------|
| Base 默认编码 | `hello` → `aGVsbG8=` | `await expect(page.locator('.base-box[readonly]')).toHaveValue('aGVsbG8=')` ✅ | ✅ |
| Base16 编码 | `hello` → `68656C6C6F` | `toHaveValue('68656C6C6F')` ✅ | ✅ |
| 解码还原 | `aGVsbG8=` → `hello` | `toHaveValue('hello')` ✅ | ✅ |
| 交互态样式（C-23） | hover 背景 violet-0 | `expect(hover).toBe('rgb(247, 243, 255)')` ✅ | ✅ |

---

## 6. 测试工具与环境

- 本地：`npm run build` 后 `BASE_URL=http://127.0.0.1:4399 npx playwright test`（config 在设 `BASE_URL` 时不启动 webServer，规避 4321 历史监听 + 沙箱代理导致的 120s 超时）。
- 失败自动留截图 + trace（`only-on-failure`）。

---

## 完成标志
- [x] 所有用例已执行（33/33）
- [x] 失败用例已修复并复测通过（2 例标签同步问题已修）
- [x] 关键断言已记录（DOM / 交互态）
- [x] 观测项验证通过，已贴原文
- [x] 已在 `00-overview.md` Progress 勾选 06
- [x] 已与用户完成结束确认
