# 06. IT (Integration Test)

> **项目适配（guoxin.space）**：本仓库无后端 / DB / MQ，**IT = 页面自动化测试（Playwright E2E，强制门禁）**，规范见 `.harness/docs/integration_test/integration_test.md` 与 `playwright.config.ts` / `e2e/`。下方通用模板残留（testcontainers / docker / req_id / trace_id）已移除，不适用。

> **目的**：在真实（或近真实）环境验证 `02-plan.md` 定义的所有 IT 用例（页面级交互 / 渲染 / 导航 / 交互态）。
> **输入**：已构建的产物（或已部署的站点）+ `02-plan.md` 的 IT 用例清单
> **输出**：每个用例的执行结果 + 关键断言证据
>
> ⚠️ **协同开发模式同样必须执行本步骤**——页面自动化聚焦本组件职责；不跳过。

---

## 0. 约束自查

> 进入本步骤前，先逐项核对以下硬约束（来自 `.harness/docs/CONSTRAINTS.md`；冲突以该文件为准）。任一项不满足，本步骤不得结束确认。

| 约束 | 规则 | 自查要点 |
|------|------|---------|
| C-11 | 页面自动化（Playwright E2E）强制门禁 | 改任何页面/交互/CSS 后必跑 `npm run test:e2e`（chromium，自动以 python3 http.server 静态服务 `app/dist`，端口 4321） |
| C-12 | CI 双门禁已落地 | `deploy.yml` build job 在 `pnpm build` 后跑 `pnpm test` + `playwright install --with-deps chromium` + `pnpm test:e2e`；任一门禁失败阻塞 deploy |
| C-13 | 禁用 `sleep` 死等 | 用 `page.waitFor*` / `expect().toBeVisible()` 自动等待；用例独立、不依赖执行顺序 |
| C-14 | 必须断言关键 DOM/交互态样式 | 改 CSS 用 `getComputedStyle` 在 `:hover`/`:focus-visible` 态回读；不只断言状态码；禁止为过 CI 关用例 |

---

## 1. 执行概览

| 项 | 值 |
|----|----|
| 环境 | 线上 `https://guoxin.space`（dispatch 部署验证）+ 用户手动步骤（Cloudflare） |
| 执行人 | AI（自动部分）/ 用户（Cloudflare Token 与部署确认） |
| 执行时间 | 2026-09-13 16:57–17:20 |
| 总用例数 | 5 |
| 通过 | 3 |
| 失败 | 0（deploy-worker 首轮失败为预期：CLOUDFLARE_API_TOKEN 未配置） |
| 跳过（含原因） | 2 待用户：Token 配置后的部署绿 + 审计/登录端到端 |

## 2.5 关键事件与用户手动清单

- **push 事件异常**：本次 push 的 deploy.yml / check-404-sync 出现 startup_failure（重跑复现），deploy-worker 排队 17 分钟未启动；workflow_dispatch 路径正常（1m10s 全绿并已上线）。已用 dispatch 完成部署验证，push 触发恢复后 rerun 即可。GitHub 状态页显示 Actions operational（疑似未上報的局部异常）。
- ⚠️ **登录中断窗口（待用户一步修复）**：新前端（hash-only）已上线，线上 Worker 仍为旧版（发 `?auth=`）→ admin 登录暂不可用。修复 = 部署新版 worker.js（二选一）：
  1. dashboard 粘贴新版 `worker.js`（约 2 分钟）；
  2. 提供具备 Workers Scripts Edit 权限的 `CLOUDFLARE_API_TOKEN`（存 GitHub Secret 后由 dispatch/rerun 部署）。
- **用户清单**：
  1. 部署新版 `worker.js`（上述二选一）；
  2. 配置 Worker Secret `SERVERCHAN_SENDKEY`（审计通知）；
  3. 配置 GitHub Secret `CLOUDFLARE_API_TOKEN`（自动部署启用）；
  4. 验证：登录 → `/#auth=` 消费 + 地址栏清理；收藏/删除 → 微信审计推送；
  5. push 触发恢复后 rerun 两个 startup_failure run。

## 2. 用例执行明细

> 对照 `02-plan.md` 第 7 节 IT 用例逐一执行（页面自动化用例）。
> **关键证据**：贴出能证明用例通过的断言结果（Playwright 报告 / 关键 DOM 文本 / 交互态样式）；仅写 ✅ 视为无效 IT。

| # | 场景 | 类型 | 关键断言（DOM / 交互态） | 结果 | 备注 |
|---|------|------|--------------------------|------|------|
| 1 | dispatch 部署（最新树） | 正向 | push 事件异常 | workflow_dispatch | ✅ 1m10s 全绿已上线 |
| 2 | 旧 query 兼容 | 逆向 | 用户确认移除 | parseAuthRedirect('?auth=…') → null | ✅ 单测 |
| 3 | fragment 登录端到端 | 正向 | **需新 Worker 部署** | 用户清单 1+4 | ⏳ 用户 |
| 4 | Worker 自动部署 | 正向 | **需 CLOUDFLARE_API_TOKEN** | 配置后 dispatch | ⏳ 用户 |
| 5 | 审计微信推送 | 正向 | **需 SENDKEY（Worker）** | 收藏/删除一次 | ⏳ 用户 |
| 2 |  | 逆向 |  | ✅ / ❌ |  |
| 3 |  | 边界 |  | ✅ / ❌ |  |

## 3. 失败定位

| # | 场景 | 失败现象 | 根因 | 修复 | 修复后状态 |
|---|------|---------|------|------|-----------|
|  |  |  |  |  |  |

> 失败处理原则：
> - 环境问题 → 协调修复后重跑，记录在备注
> - 代码问题 → 修复代码 → 按 `05-deploy.md` §5 amend + `--force-with-lease` 重新部署 → 复测本步骤失败用例（必要时重跑 UT），**循环直到全部用例通过**，再进入 07 Docs
> - 设计问题 → 回到 02-plan 修改方案，按需回退更多阶段

## 4. 观测验证

> 本次改动涉及的页面行为是否都按预期出现。
> **「实际」列必须贴出断言 / Playwright 报告原文**，不得仅写"符合预期"。

| 项 | 预期 | 实际（贴原文） | 通过 |
|----|------|---------------------|------|
| 关键 DOM |  |  | ⬜ |
| 交互态样式 |  |  | ⬜ |

## 5. 性能 / 压测（若需要）

| 项 | 预期 | 实际 |
|----|------|------|
| 首屏加载 |  |  |
| Lighthouse（可选） |  |  |

## 6. 测试工具与环境

> 页面自动化统一用 **Playwright**（见 `.harness/docs/integration_test/integration_test.md`）：
> - 本地：`npm run build && npm run test:e2e`（webServer 自动以 python3 http.server 静态服务 `app/dist`）
> - 线上复验：`BASE_URL=https://guoxin.space npx playwright test`（不启动本地服务）
> - 失败自动留截图 + trace（`only-on-failure`），作为追溯证据
> - 禁止用 `sleep` 死等；用 `page.waitFor*` / `expect().toBeVisible()` 自动等待

| 操作 | 做法 |
|------|------|
| 起服务 | `npm run build` 后由 Playwright webServer 自动起 python3 http.server（静态服务 app/dist） |
| 跑用例 | `npm run test:e2e`（全量）/ `npx playwright test e2e/xxx.spec.ts`（单文件） |
| 清理 | 用例独立、无共享状态；Playwright 每次运行全新上下文 |

## 7. 报告 / 任务链接

- 测试报告：`playwright-report/`（HTML，CI 生成）
- 任务 ID / Pipeline：`gh run view <run-id>`

---

## 决策框架

1. **真实渲染优先**：用真实构建产物 / 线上站点验证，不用构造数据假装通过。
2. **异常注入**：主动触发 404 / 空数据 / 网络不可达（如 Running Worker 阻断），看页面降级是否符合预期。
3. **问题不留尾**：所有 ❌ 用例必须有根因 + 修复 + 复测，不能"稍后补"。
4. **断言为证**：每个用例必须贴出关键断言（DOM 文本 / 交互态 `getComputedStyle` 结果）或 Playwright 报告；没有证据 = 没做 IT = 驳回重测。

---

## 反例

❌ **反例 1：失败用例"下次修"**
> 10 个用例 3 个失败，写"已知问题，不影响合入"。

✅ **原则**
> 要么修掉，要么明确降级为已知限制并在 Docs 中登记，不能糊过去。

❌ **反例 2：没贴断言证据**
> 用例写了 ✅，但"关键断言"栏为空或仅写"正常"/"符合预期"。

✅ **原则**
> 每个用例必须粘贴能证明通过的关键断言（1-3 行：DOM 文本 / 交互态样式 / Playwright 报告片段）。没有证据 = 驳回重测。

❌ **反例 3：为让 CI 过而关用例**
> 把不稳定用例标 skip 让流水线绿。

✅ **原则**
> 用例不稳定先修用例 / 修代码，禁止静默 skip。

---

## 完成标志

- [ ] 所有用例已执行
- [ ] 失败用例已修复并复测通过（代码修复经 05 的 amend + force-push 重部署闭环，无未复测修复）
- [ ] 关键断言已记录（DOM / 交互态）
- [ ] 观测项验证通过，已贴原文
- [ ] 已在 `00-overview.md` Progress 勾选 06
- [ ] 已与用户完成结束确认
