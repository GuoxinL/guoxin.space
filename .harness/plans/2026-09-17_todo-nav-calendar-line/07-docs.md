# 07. Docs

> **目的**：同步与代码行为相关的文档/规范，避免文档漂移。
> **输入**：`03-implement.md` · **输出**：本文件 + 被更新的文档

---

## 0. 约束自查（强制）

| 约束 | 规则 | 自查要点 | 结论 |
|------|------|---------|------|
| C-01 | 文档与代码一致 | 导航结构（TODO 位置）、单测计数已同步 | ✅ |
| C-54 | Notes 数仓脚本/示例文档变更须同步 `example` 分支 | 本任务**未**触及 notes 数仓脚本/示例文档 | ✅ 不适用 |

---

## 1. 文档更新清单

| # | 文档 | 改动 | 原因 |
|---|------|------|------|
| 1 | `README.md` | ① 开发命令区的单测计数 `16 文件 / 265 用例` → `22 文件 / 329 用例`；② 里程碑追加「2026-09-17（晚）：TODO 入口上移 + 日历任务线连续化」 | 命令区计数已严重滞后；里程碑需反映本次功能变化 |
| 2 | `AGENTS.md` | `/todo` 条目补充「入口在主导航 TODO 项——登录后才显示，桌面/移动端一致」 | 导航入口位置变化，AI 接手时需知道 TODO 不在 Toolbox 里 |
| 3 | `app/src/global.css` | 在 `.cal-todo*` 规则块上补写「为什么这样写」的设计注释（底部对齐 / 禁 `width:100%` / 贴边量 -8px 与 -9px 的来历） | 该 CSS 是本任务最易被后人改坏的处（改动看起来等价，实则破坏连线），注释承担护栏职责 |
| 4 | `app/src/lib/calendar/todo-line.ts` | 文件头写明「按行分配 lane」的动机与跨行语义 | 纯函数模块的设计意图无法从代码直接读出 |
| 5 | `.harness/plans/2026-09-17_todo-nav-calendar-line/` | 新增 SOP 8 步计划文件 | 项目铁律（任何改动先走 SOP） |

## 2. 经核查**无需**更新的文档

| 文档 | 核查结论 |
|------|---------|
| `DESIGN.md` | 视觉设计真源中**没有**日历/TODO 条目（grep 无命中）；本次线段圆角由 3px 收敛为令牌值 999px，符合 C-35 既有约定，未引入新令牌/新色值，故不新增章节 |
| `.harness/docs/CONSTRAINTS.md` | 未触碰任何红线（C-21 未动 `.btn`、C-39 未改宽度单点、C-35 更合规）；无新增/修订约束 |
| `docs/third-party/*` | 未涉及 Worker / Pages / Server酱 / 行者接入变化 |
| `docs/reports/TOOLBOX-SELFTEST.md` | 自测报告未描述「日历任务线」条目（grep 无命中），无内容需同步 |
| `running-private/*` | 完全不涉及 |
| `.harness/docs/relationship.md` 等 | 未涉及域名/数据流变化 |

## 3. 文档一致性验证

- `README.md` 的计数与实测一致：`npm run test` → `22 passed (22) / 329 passed (329)`。
- `AGENTS.md` 的 `/todo` 描述与 `Header.tsx` / `ToolboxTabs.tsx` 实际行为一致（TODO 不在 `TOOLS` 中）。
- `global.css` 注释中的数值（`-8px` / `-9px` / `margin-top: auto`）与规则本体一致。

---

## 完成标志

- [x] 文档更新清单完整
- [x] 无需更新的文档已核查并说明
- [x] 文档与代码一致（计数、导航结构、CSS 数值）
- [x] `00-overview.md` Progress / 时间记录已同步
