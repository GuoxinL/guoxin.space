# 04. UT

> **目的**：记录单元测试用例、执行结果与未覆盖点。
> **输入**：`03-implement.md` · **输出**：本文件 + 测试文件
> **红线**：UT 全 Mock，不调用真实 Worker（C-15）。

---

## 0. 约束自查（强制，详见 `.harness/docs/CONSTRAINTS.md`）

| 约束 | 规则 | 自查要点 | 结论 |
|------|------|---------|------|
| C-10 | 改 `app/src/lib/` 必跑 `npm run test` | 新增 `lib/calendar/todo-line.ts` → 已跑全量 `npm run test` | ✅ 329/329 |
| C-15 | UT 全 Mock，不调真实外部服务 | 被测对象是纯函数（零外部依赖），无需 mock；未引入任何网络调用 | ✅ |

## 0.5 UT / IT 边界

- **UT 覆盖**：lane 分配算法、跨行开放标记、段→格片段展开、边界与脏数据（纯函数层，输入输出完全确定）。
- **IT 覆盖**：真实 DOM 渲染 + CSS 几何（相邻格线段是否真的无缝、同 lane 是否等高）+ hover 态样式回读 + 导航登录门控。
- 两者不重复：UT 不碰 DOM/CSS，IT 不重复枚举算法分支。

---

## 1. 用例清单（`app/src/lib/calendar/todo-line.test.ts`）

| # | 用例 | 类型 | 结果 |
|---|------|------|------|
| 1 | `dayKey` 月/日补零 | 正向 | ✅ |
| 2 | 单日任务：只有该格有片段、两端圆角 | 正向 | ✅ |
| 3 | 行内跨日：中间格两端贴边、首末格各开放一侧 | 正向 | ✅ |
| 4 | `endDate=null` 按单日处理 | 边界 | ✅ |
| 5 | 任务在本行首列前已开始 → 首格左端仍贴边（承接上一行） | 边界 | ✅ |
| 6 | 任务在本行末列后未结束 → 末格右端仍贴边（延续下一行） | 边界 | ✅ |
| 7 | 任务完整覆盖本行 → 每格两端贴边 | 边界 | ✅ |
| 8 | 3 个任务覆盖同一列 → lane 0/1/2，无溢出 | 正向 | ✅ |
| 9 | 4 个任务覆盖同一列 → 溢出 1，仅渲染 `CAL_MAX_LANES` 条 | 边界（上限） | ✅ |
| 10 | 不重叠任务复用同一 lane | 正向 | ✅ |
| 11 | 同起点时长者优先（长任务占 lane 0） | 正向 | ✅ |
| 12 | 无 TODO → 全空布局但给出 `homeCol` | 边界（空） | ✅ |
| 13 | 整行不可用（全补白格）→ 空布局且 `homeCol=-1` | 边界 | ✅ |
| 14 | `startDate > endDate` 脏数据 → 不产生片段 | 逆向 | ✅ |
| 15 | 完全落在本行外的 TODO → 不产生片段 | 逆向 | ✅ |
| 16 | 补白格不参与：仅在 enabled 列上算起止与开放端 | 边界 | ✅ |

## 2. 执行记录

```
> npx vitest run app/src/lib/calendar/todo-line.test.ts
 ✓ src/lib/calendar/todo-line.test.ts  (16 tests) 12ms

> npm run test
 Test Files  22 passed (22)
      Tests  329 passed (329)
   Duration  6.38s
```

- 新增文件用例：**16/16 绿**
- 全量回归：**329/329 绿**（本任务前基线 313 → 新增 16）

## 3. 未覆盖点 / 说明

| 项 | 说明 |
|----|------|
| `layoutTodoRow` 的 lane 分配最优性 | 采用贪心（非最优装箱），但「同起点时长优先」已覆盖常见情形；UT 只断言确定性行为，不断言全局最优 |
| 溢出的具体丢弃顺序 | 由排序决定（起点升序 → 时长降序），UT 只断言数量（3 条 + overflow=1），不断言丢哪一条 |
| 组件层渲染 | 属 IT 范畴（`e2e/calendar.spec.ts`），不在 UT 覆盖 |

## 4. 覆盖率

未单独采集覆盖率工具输出（项目 `vitest` 未配置 `--coverage`）。以「核心函数 + 关键分支全覆盖」为准：`dayKey`、`layoutTodoRow` 的全部返回分支（空列 / 空 todos / 有候选 / 溢出 / 复用 lane）均有对应用例。

---

## 完成标志

- [x] UT 用例全部通过（16/16 新增 + 329/329 全量）
- [x] 覆盖 正向 / 逆向 / 边界 三类
- [x] 未覆盖点已说明理由
- [x] `00-overview.md` Progress / 时间记录已同步
