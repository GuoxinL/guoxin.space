# 04. UT

> **项目适配（guoxin.space）**：本仓库 UT 用 **Vitest**；规范与环境见 `.harness/docs/unittest/unittest.md`。

> **目的**：以单元测试证明行为正确、回归可防。
> **输入**：`02-plan.md §6` UT 用例设计 + `03-implement.md` 完成的代码
> **输出**：测试文件 + 覆盖率 + 本文件

## 0. 约束自查（强制，详见 `.harness/docs/CONSTRAINTS.md`）

- 测试门禁：C-10（UT 强制，改 lib 必跑）✅、C-15（全 Mock / 用例独立 / 不删测试 / 不硬编码环境）✅
- 编码：C-30（禁 any）✅

---

## 0.1 前置阅读（强制）

1. **`02-plan.md` §6 UT 用例设计** — 本阶段用例基线，§1 表逐条对齐 ✅
2. **`.harness/docs/unittest/unittest.md`** — 单元测试规范（Vitest 环境）✅
3. **Vitest 运行环境** — Node 22.22.2（`npm run test`；单测目录 `app/src/{lib,components}/**`）✅

---

## 0.5 TDD 工作流自检

| 阶段 | 期望状态 | 当前情况 |
|------|---------|---------|
| 🔴 Red（Implement 前/中） | Plan §6 列出的 UT 已先写、首次跑全部失败 | ⬜ 跳过（说明原因） |
| 🟢 Green（Implement 后） | 实现使所有 UT 转绿 | ✅ 已完成 |
| 🔵 Refactor | 在 UT 保持绿的前提下重构代码 | ✅ 无需重构 |

> Red 跳过说明：本任务为 hotfix（线上功能失效），直接改实现后补/改断言，未先制造红灯。属模板允许的 hotfix 例外。

---

## 1. 用例清单

| # | Plan §6 编号 | 用例名 | 测试文件 | 类型 | 状态 |
|---|-------------|--------|---------|------|------|
| 1 | §6-1 | `isTodoAuthed` 在 admin + Worker 已配时为真 | `lib/todo/api.test.ts` | 正向 | ✅ |
| 2 | §6-2 | `isTodoAuthed`：未登录时为假 | `lib/todo/api.test.ts` | 逆向 | ✅ |
| 3 | §6-2 | `isTodoAuthed`：Worker 地址为空时为假 | `lib/todo/api.test.ts` | 边界 | ✅ |
| 4 | §6-3 | `fetchMonth` 解析月索引（URL 含 `y`/`m`） | `lib/todo/api.test.ts` | 正向 | ✅ |
| 5 | §6-4 | `fetchAll` / `fetchTags` 解析 | `lib/todo/api.test.ts` | 正向 | ✅ |
| 6 | §6-4 | `saveTags` POST 携带标签 | `lib/todo/api.test.ts` | 正向 | ✅ |
| 7 | §6-5 | `saveDay` 遇 401 抛未登录错误 | `lib/todo/api.test.ts` | 异常 | ✅ |
| 8 | §6-6 | `saveDay` 连续两次相同日文件均成功（幂等） | `lib/todo/api.test.ts` | 幂等 | ✅ |

## 2. 执行命令

```bash
npm run test                      # 全量（CI 同款）
npx vitest run src/lib/todo       # 只跑 todo 逻辑层
```

## 3. 结果

| 项 | 值 |
|----|----|
| 通过 / 总数 | 310 / 310（全量，21 文件）；todo 逻辑层 45 / 45（5 文件） |
| 本文件用例数 | `api.test.ts` 8 / 8 |
| 覆盖率 | 未单独采集（项目无强制阈值）；新增 2 例覆盖 `isTodoAuthed` 逆向 + 边界分支 |
| 门槛 | 无强制覆盖率阈值；新逻辑均有对应用例 |

**未覆盖行**：
```
`lib/todo/api.ts:26/28`（todoGet 中 `!base` 分支）—— authWorkerUrl() 带默认值恒非空，本仓实际不可达；不强行用 mock 制造。
```

## 4. 失败定位

| 用例 | 失败原因 | 修复动作 | 修复后状态 |
|------|---------|---------|-----------|
| — | 无失败 | — | — |

---

## 完成标志

- [x] 所有用例通过（310/310）
- [x] 新逻辑有对应用例（新增 2 例）
- [x] 未覆盖行有说明
- [x] 无修改被测业务代码以绕过失败的行为
- [x] 新增 Mock / step 仅追加到文件尾部
- [x] 已在 `00-overview.md` Progress 勾选 04.
- [x] 已与用户完成结束确认
