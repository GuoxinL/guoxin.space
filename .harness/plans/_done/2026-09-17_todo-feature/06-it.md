# 06. IT (Integration Test)

> **目的**：Playwright 页面自动化验证 `02-plan.md §7` 用例。IT 走本地 `app/dist`（先 build）或线上。

## 0. 约束自查
- C-11（E2E 强制）、C-12（双门禁）、C-13（禁 sleep）、C-14（断言 DOM/交互态样式，hover 态回读）

## 1. 执行概览
| 项 | 值 |
|----|----|
| 总用例数 | 9（见 02-plan §7） |
| 通过 / 失败 / 跳过 | 待执行（spec 已落 `e2e/todo.spec.ts`；需 chromium + Cloudflare `TODO_REPO` secret 方可跑通鉴权流） |

## 2. 用例执行明细
| # | 场景 | 类型 | 关键断言 | 结果 |
|---|------|------|---------|------|
| 1 | 未登录 `/todo` | 逆向 | 登录引导、无数据 | spec 已写・待执行 |
| 2 | 创建 TODO | 正向 | 卡片+进度条+POST `/api/todo/save` | spec 已写・待执行 |
| 3 | 编辑/重开/硬删 | 正向 | 列表更新 | 并入 #2 弹窗流・待执行 |
| 4 | 标签 OR + 进度 + 排序 | 正向 | 过滤排序正确 + URL 同步 | spec 已写・待执行 |
| 5 | 周报导出 | 正向 | 三格式弹窗 | spec 已写・待执行 |
| 6 | 日历线条 | 正向 | hover 浮窗+深链 | spec 已写・待执行 |
| 7 | URL 同步 | 边界 | 写入侧 `?tag=/?filter=` 同步 | spec 已写・待执行（⚠️ 加载回读未实现，刷新不保留） |
| 8 | 暗黑模式 | 正向 | 暗色变量生效 | spec 已写・待执行 |
| 9 | Worker 故障 | 异常 | 错误态不白屏 | spec 已写・待执行 |

## 3. 失败定位 / 4. 观测验证 / 6. 测试工具
- spec 写入 `e2e/todo.spec.ts`：`page.route` mock Worker + `addInitScript` 注入 localStorage（`worker_url`/`wb_home_auth_token`/`wb_home_gh_user`）模拟登录态，离线确定。
- ⚠️ 已知缺口（见 08-review #1）：`TodoPage` 仅把筛选/排序写入 URL，加载时不回读 → 「刷新保留筛选态」未实现。

## 完成标志
- [ ] 9 例全执行；失败已修复复测（待 chromium + Cloudflare secret 就绪后跑 `npm run test:e2e`）
- [x] spec 已落库（`e2e/todo.spec.ts`）
