# 06 · 集成 / 生产验证

## Worker 层（已完成的实测，Playwright chromium `--no-proxy-server` 直连）
| URL | 结果 |
|---|---|
| `https://guoxin-space.lgx31.workers.dev/api/health` | **200** `{ok:true, repo:"guoxinl/skill-collection", admin:true}` → 变量保留 |
| `https://guoxin-space.lgx31.workers.dev/api/tracks/raw?f=preview.json` | **200** 真实轨迹数据 → `TRACKS_REPO` 变量在 |
| `https://skillboard-collect.lgx31.workers.dev/api/health` | **404** → 旧域确认失效 |

> 沙箱 `curl` 直连 workers.dev 被拦截（返回 000），改用 Playwright 浏览器 `--no-proxy-server` 直连验证。

## 站点层（待补）
- 访问 `https://guoxin.space/todo`：登录门禁正常，登录后 TODO 读写经 `guoxin-space.lgx31.workers.dev`。
- 访问 `https://guoxin.space/skills`：列表加载 + 「测试连接」绿勾。
- 访问 `https://guoxin.space/running`：轨迹 preview 加载正常（CI e2e 亦覆盖此页）。

## Worker 端点全套测试（2026-09-17 21:35，Playwright 直连）
| 测试 | 结果 |
|---|---|
| `/api/health` | 200，`permissions:{admin:true,push:true,maintain:true,...}` → **GH_TOKEN(classic) 有效且有写权限** |
| `/api/tracks/raw?f=preview.json` | 200（私仓读 OK） |
| `/api/tracks/raw?f=rides.full.json`（无 token） | 401 ✓ |
| `/api/auth/me`（无 token） | 401 ✓ |
| `/api/auth/callback?code=bogus` | `换取 access_token 失败：The code passed is incorrect or expired.` → **callback 已通到 GitHub** |
| POST `/api/collect` / `/api/todo/save`（无 token） | 401 ✓ |
| `/api/auth/login` | 302 → `redirect_uri=https://guoxin-space.lgx31.workers.dev/api/auth/callback`（**与配置一致**） |

## 真实登录态测试（2026-09-17 21:40，bsk 驱动已登录夸克）
- 用户浏览器已登录：`wb_home_auth_token` 有效 → `GET /api/auth/me` **200** `{ok:true,login:"GuoxinL",exp:...}`。
- `GET /api/todo/all`（带真实 token）→ **200** `{ok:true,todos:[]}`（仓库当前 0 条）。
- **发现并修复**：老用户 localStorage `wb_home_sk_set.worker` 存有死域 `skillboard-collect...` → `Failed to fetch`；新增 `skMigrateWorker()` 自动迁移，实测旧域 → 新域生效。

## 状态
- [x] Worker 默认域可达、变量齐全
- [x] GH_TOKEN(classic) 有效 + 写权限；OAuth login/callback 通
- [x] 站点部署 e2e 转绿：run **35225973512 success**（+ 迁移修复 run **35228799879 success**）
- [x] 真实 token 端到端：`/api/auth/me` 200、`/api/todo/all` 200
- [x] 旧域名 localStorage 自动迁移实测生效
- （用户已配置 OAuth callback + GH_TOKEN classic）
