# 04 · 单元测试（门禁）

触发：改动 `app/src/lib/skills.ts`（`SK_DFLT_WORKER` 常量）→ 必跑 `pnpm test`。

## 迭代记录
1. 首跑：1 失败 — `auth.test.ts:89` 断言默认 Worker 含 `workers.dev`，与改后的 `api.guoxin.space` 不符。
2. 修复：`auth.test.ts:89` → `toContain('api.guoxin.space')` → **310/310 通过**。
3. 决策变更后（改用默认域）：`auth.test.ts:89` / `running.test.ts:284` 断言 → `toContain('guoxin-space.lgx31.workers.dev')`。

## 结果
`CI=true pnpm test` → **Test Files 21 passed · Tests 313 passed** ✅（新增 `skMigrateWorker` 旧域名迁移 3 条：旧域→新默认域、新域/自定义域原样、`loadSkCfg` 读改写回）。

## 说明
- `worker.test.mjs`（根目录 Worker 脚本级）不在 vitest 门禁内；Worker 行为以线上探测验证（见 06-it）。
- 改 `SK_DFLT_WORKER` 会牵连**多处硬编码断言**（running/auth），改常量必须全局搜断言。
