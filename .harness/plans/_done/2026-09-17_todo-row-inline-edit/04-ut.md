# 04. UT

> **项目适配**：本仓库 UT 用 **Vitest**（`npm run test`，实际执行 `./node_modules/.bin/vitest run`，须在仓库根跑）。静态站无后端 / DB，依赖一律 mock。
> 状态：✅ 完成　2026-09-17 23:34

## 0. 约束自查

- C-10（改 lib 必跑 UT）✅：`mutate.ts` / `write-queue.ts` 均有对应用例
- C-15（全 Mock / 用例独立 / 不删测试）✅：新增用例只追加，未删既有
- C-30（禁 `any`）✅、C-31（Qwik 原语）✅：被测均为纯 TS，不依赖框架

## 0.5 TDD 工作流自检

| 阶段 | 状态 |
|------|------|
| 🔴 Red | ✅ 已先写 `mutate.test.ts` / `write-queue.test.ts` 并跑红 |
| 🟢 Green | ✅ 实现后全部转绿 |
| 🔵 Refactor | ✅ 队列 `start()` 重构为内部 catch + `lastError`（修 unhandled rejection），UT 保持绿 |

## 1. 用例清单

| # | 用例组 | 测试文件 | 例数 | 状态 |
|---|--------|---------|------|------|
| 1 | `localDay`（本地日期口径 / 跨月 / 北京时间 0–8 点不回退） | `app/src/lib/todo/mutate.test.ts` | 3 | ✅ |
| 2 | `patchTodo`（title / tags / startDate / endDate→null） | 同上 | 4 | ✅ |
| 3 | `closeTodo`（子任务全 100% / 写 completedAt / 无子任务 / 幂等） | 同上 | 4 | ✅ |
| 4 | `reopenTodo`（只清 completedAt，子任务进度不变） | 同上 | 1 | ✅ |
| 5 | `withSubtasks`（进度重算 / 自动完成 / 取消完成 / 保留字段） | 同上 | 5 | ✅ |
| 6 | 写盘队列：单次写出、连续 push 合并、超 debounce 各写、串行尾写 | `app/src/lib/todo/write-queue.test.ts` | 4 | ✅ |
| 7 | 写盘队列：`flush` 立即写 / 空 flush / 失败冒泡不卡死 | 同上 | 3 | ✅ |
| 8 | 写盘队列：debounce 失败不 unhandled + flush 冒泡 / `dispose` 清 timer / `onError` 上报 | 同上 | 3 | ✅ |

合计 **27 例**（Plan 预估 18，实际多拆了队列的失败与合并路径）。

## 2. 执行命令

```bash
cd /Users/guoxin/code/github/guoxin.space
export PATH="$HOME/.nvm/versions/node/v24.20.0/bin:$PATH"
./node_modules/.bin/vitest run                      # 全量
./node_modules/.bin/vitest run app/src/lib/todo     # 只跑本次改动
```

## 3. 结果

| 项 | 值 |
|----|----|
| 通过 / 总数 | **356 / 356**（24 文件） |
| 本次新增 | 27 例，全绿 |
| 覆盖率门槛 | 项目当前无强制阈值；新逻辑（mutate / write-queue）逐函数有用例 |

**未覆盖行**：
- `write-queue.ts` 的 `dispose()` 之后再次 `push` 的极端路径——属防御性分支，无用例（组件已卸载即不再 push）。
- `TodoRow` / `TodoPage` 的交互逻辑不进 UT，由 e2e（`e2e/todo.spec.ts`）覆盖，见 `06-it.md`。

## 4. 失败定位

| 用例 | 失败原因 | 修复动作 | 状态 |
|------|---------|---------|------|
| — | 本次无失败 | — | — |

> 附带修复：lint 报 10 处 `_v is defined but never used`（测试里的占位参数）→ 改为无参形式 `vi.fn(async () => {})`（可安全赋值给 `(value: string) => Promise<void>`）。

---

## 完成标志

- [x] 所有用例通过（356/356）
- [x] 新逻辑均有对应用例
- [x] 未覆盖行已说明
- [x] 未改业务代码绕过失败
- [x] 新增用例仅追加，未删既有
- [x] `00-overview.md` Progress 已勾选 04.
- [ ] 已与用户完成结束确认（待 08）
