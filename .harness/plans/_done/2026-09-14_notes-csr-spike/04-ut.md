# 04. UT

> **目的**：纯函数级验证，全 Mock。

---

## 1. 红绿循环记录

| 阶段 | 命令 | 结果 |
|------|------|------|
| **Red #1** | `npx vitest run app/src/lib/notes/slug.test.ts` | ❌ `Failed to load url ./slug ... Does the file exist?` —— 1 failed suite / 0 tests（模块尚未创建） |
| **Green #1** | 同上 | ✅ `(10 tests) 7ms` —— **10 passed** |
| **Red #2**（方案 A 追加 `resolveInitialSlug`） | 同上 | ❌ **5 failed / 10 passed**（`resolveInitialSlug is not a function`） |
| **Green #2** | 同上 | ✅ `(15 tests) 13ms` —— **15 passed** |

> 两轮红绿均实测留证，非推断。

## 2. 用例结果

| # | 类型 | 场景 | 结果 |
|---|------|------|------|
| 1 | 正向 | `/notes/测试笔记/` → `测试笔记` | ✅ |
| 2 | 正向 | 百分号编码中文解码（带/不带尾斜杠） | ✅ |
| 3 | 边界 | `/notes/`、`/notes`、`/`、`''`、`/running/`、`/toolbox/json` → `''` | ✅ |
| 4 | 逆向 | 畸形编码 `/notes/%zz/` 不抛异常，返回 `%zz` | ✅ |
| 5 | 边界 | 双重编码只解一次 → `%E6%B5%8B` | ✅ |
| 6 | 边界 | catch-all 多级 `/notes/分类/测试笔记/` → `分类/测试笔记` | ✅ |
| 7 | 边界 | 多余尾斜杠剥离 | ✅ |
| 8 | 正向 | `notePathFor('测试笔记')` → `/notes/%E6%B5%8B%E8%AF%95%E7%AC%94%E8%AE%B0/` | ✅ |
| 9 | 正向 | `notePathFor('')` → `/notes/` | ✅ |
| 10 | 幂等 | 往返一致 `noteSlugFromPath(notePathFor(s)) === s`（中文/空格/含斜杠/C++） | ✅ |
| 11 | 正向 | `resolveInitialSlug`：暂存了笔记路径 → 返回 slug + 待修正 URL | ✅ |
| 12 | 边界 | 无暂存（普通访问）→ 按当前 pathname 解析，`restoreUrl: null` | ✅ |
| 13 | 边界 | 暂存值本身无 slug（`/notes/`）→ 回退到列表 | ✅ |
| 14 | 逆向 | 暂存值非 notes 路径（`/skills/foo`）→ 回退到当前 pathname | ✅ |
| 15 | 边界 | 未知 slug 同样修正 URL（由 UI 显示「未找到」） | ✅ |

**合计 15 用例，全通过。**

> 用例 11–15 为**方案 A（404 引导页）**追加，覆盖深链恢复分支。

## 3. 全量回归

```
npm run test
 Test Files  10 passed (10)
      Tests  171 passed (171)
```

> 10 文件 / **171 用例**全绿（原有 156 + 新增 slug 15），无回归。

## 4. 未覆盖说明

- `NotesShell.tsx` 的交互（点击 / `popstate`）属**调用链**层，按 UT/IT 分工由 IT 覆盖（`06-it.md`），不在此重复测。
- 覆盖率：本次未单独统计；`slug.ts` 为纯函数且分支少（命中/未命中/解码异常三分支均有用例）。
