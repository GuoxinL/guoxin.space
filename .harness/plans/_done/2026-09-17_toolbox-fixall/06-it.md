# 06 IT — 页面自动化（Playwright）

执行：`npm run build` 后 `npm run test:e2e`（serve-pages 模拟 GitHub Pages 语义，含 404 fallback）。

## 结果：✅ 50 passed / ❌ 2 failed

- 失败 1：`e2e/running.spec.ts:12` — Running 年度热力图依赖 Cloudflare Worker 代理数据；沙箱浏览器无外网，环境性失败（与本次改动无关）。
- 失败 2：`e2e/notes.spec.ts:68` — Notes 详情页依赖 `raw.githubusercontent.com/GuoxinL/notes` 文章数据；沙箱浏览器系统代理端口无服务，环境性失败（与本次改动无关）。

## 本次新增/扩展 e2e（全绿）
- `e2e/toolbox-json.spec.ts`：2 → 10 例（渲染 / 格式化 / 语言互转 / 修复传 lang / JSONPath 高亮 / 历史 / 分享 / 提取 / 小工具 / 结构对比）均 ✅。
- `e2e/calendar.spec.ts`：6 例（子导航 / 月视图 42 格 / 休班徽标 / 上下月切换 / 今日高亮 / 未维护年份提示）均 ✅，未因年视图/tooltip/跨月跳改改动而回归。

## C-23 回归回读（getComputedStyle，hover/focus 态）
独立探针（chromium）验证新交互 CSS 生效：
- `.cal-out.cal-link:hover` → `rgb(247,243,255)`（violet-0）✅
- `.cal-view-btn.active` → `rgb(160,83,254)`（violet-65）✅
- 年视图渲染 12 个 `.cal-mini` ✅
- `.tools-tab.active` → `rgb(247,243,255)`（violet-0）✅
