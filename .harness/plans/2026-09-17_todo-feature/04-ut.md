# 04. UT

> **目的**：以单元测试证明行为正确。逻辑层 32 用例已绿（progress/filter/weekly）；本步骤补 `api.test.ts`（Mock Worker）、`store.test.ts`（store 纯函数边界）。

## 0. 约束自查
- C-10（UT 强制）、C-15（全 Mock / 用例独立 / 不硬编码环境）、C-30（类型严格）、C-31（Qwik 原语）

## 1. 用例清单
| # | Plan §6 编号 | 用例名 | 测试文件 | 类型 | 状态 |
|---|-------------|--------|---------|------|------|
| 1 | §6-1~6 | 进度/筛选/周报 | progress/filter/weekly.test.ts | 正向/逆向/边界 | ✅ 已绿（32） |
| 2 | §6-7 | store 纯函数边界（缺 createdAt 跳过 / 非法日期兜底 / 加权摘要） | store.test.ts | 边界 | ✅ |
| 3 | §6-8 | api.fetchMonth 解析（URL 含 /api/todo/month&y=2026&m=9） | api.test.ts | 正向 | ✅ |
| 4 | §6-9 | api.saveDay 遇 401 抛「未登录」 | api.test.ts | 异常 | ✅ |
| 5 | §6-10 | api.saveDay 连续两次相同日文件均 POST 成功（幂等） | api.test.ts | 幂等 | ✅ |
| 6 | — | api.fetchAll/fetchTags/saveTags + isTodoAuthed | api.test.ts | 正向 | ✅ |

## 2. 执行命令
```bash
npm run test
```

## 3. 结果
- 全量 `npm run test`：**308 passed（21 files）**。
- todo 逻辑层：**43 passed**（progress 11 / filter 10 / weekly 11 / store 5 / api 6）。
- `prettier --check`：仅 2 个新测试文件需格式化，已 `prettier --write` 修复；源码文件均合规。
- `eslint`（scoped 到改动文件）：0 error。`tsc --noEmit`（改动文件）：0 error。

## 4. 失败定位
无（全部通过）。

## 完成标志
- [x] 所有用例通过；新逻辑（api/store）有对应用例；`@harness` 一致性清扫
